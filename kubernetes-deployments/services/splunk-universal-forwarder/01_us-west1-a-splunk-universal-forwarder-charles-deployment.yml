
kind: Deployment
apiVersion: extensions/v1beta1
metadata:
  name: splunk-universal-forwarder-charles
  labels:
    app: splunk-universal-forwarder-charles
spec:
  replicas: 1
  selector:
    matchLabels:
      app: splunk-universal-forwarder-charles
  template:
    metadata:
      labels:
        app: splunk-universal-forwarder-charles
    spec:
      volumes:
        - name: task-pv-storage
          persistentVolumeClaim:
            claimName: task-pv-claim
      containers:
      - name: splunk-universal-forwarder-charles
        image: splunk/universalforwarder:latest
        ports:
        - containerPort: 9997
        env:
          - name: SPLUNK_START_ARGS
            value: --accept-license
          - name: SPLUNK_USER
            value: root
          - name: SPLUNK_PASSWORD
            value: Changeme
        volumeMounts:
        - mountPath: "/var/log/challenge1"
          name: task-pv-storage
              